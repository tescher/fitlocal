run_fitlocal.bat
.env
__pycache__/
*.pyc
instance/

# Runtime artifacts
*.log
session_*.json

# Stray SQLite file at repo root — the real DB lives in instance/ (Flask
# resolves the relative sqlite:/// URI to the instance folder)
/fitlocal.db
